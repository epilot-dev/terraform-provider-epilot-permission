terraform {
  required_providers {
    epilot-permission = {
      source  = "epilot-dev/epilot-permission"
      version = "0.0.3"
    }
  }
}

provider "epilot-permission" {
  # Configuration options
}